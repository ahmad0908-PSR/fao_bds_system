# Copy this file to ".env" and fill in real values.
# Never commit the real ".env" file to git.

# Path to the SQLite database file (relative to project root)
DATABASE_PATH=data/bds_system.db

# Bcrypt hash of your Data Entry App login password.
# Generate one by running: python shared/generate_password_hash.py
ENTRY_APP_PASSWORD_HASH=
